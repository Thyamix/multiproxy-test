apiVersion: apps/v1
kind: Deployment
metadata:
  name: controller
  namespace: multiproxy-test
spec:
  selector:
    matchLabels:
      app: controller
  replicas: 1
  template:
    metadata:
      labels:
        app: controller
    spec:
      containers:
        - name: controller
          image: multiproxy-test:local

